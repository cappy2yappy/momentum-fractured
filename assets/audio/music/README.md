# Music Assets

`scripts/systems/audio_manager.gd` will load these tracks when present:

- `combat_theme.ogg`
- `exploration_theme.ogg`
- `boss_theme.ogg`

If a track is missing, AudioManager falls back to synthesized looping tones.
